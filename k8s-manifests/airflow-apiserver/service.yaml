apiVersion: v1
kind: Service
metadata:
  name: airflow-apiserver
  namespace: default
spec:
  selector:
    app: airflow-apiserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
